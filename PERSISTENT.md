# PERSISTENT.md

*Cross-chat state. Read before writing any Claude Code prompt. Updated at the end of every session.*

*Section 7 is cleared and rewritten every session. **Section 8 is not** — it holds work items owned by stages more than one session away, and it survives clearing. A session that rewrites section 7 must leave section 8 alone except to add to it, change a Status, or close an item.*

**Last updated:** 2026-09-19 (Stage 2 complete)
**Current stage:** 2 — complete; Stage 3 is next
**Primary device:** GTX 1650 Ti (D1 resolved)

---

## 1. Open decisions

| # | Decision | Blocks | Status |
|---|---|---|---|
| D1 | Primary measurement device | everything | **RESOLVED — GTX 1650 Ti, fixed for project life** |
| D2 | Numerical tolerance for the correctness gate | Stage 3 | open |
| D3 | Prompt set and token counts for benchmarking | Stage 3 | open — **Stage 0 flag:** the cuBLAS reference in `bench/microbench/cublas_sgemm_ref.cu` swept M across 1, 8, 16, 32, 64, 128, 256, 512, 1024 as a placeholder. Stage 3 fixes the real prompt set and token counts, and every later stage holds them constant |
| D4 | Sequence lengths for the flash attention memory sweep | Stage 9 | open |
| D5 | Performance model structure — which terms to include | Stage 10 | open, decide at Stage 10 |
| D6 | Which optional stages to build | after Stage 12 | open |
| D7 | Whether to attempt Stage 16 upstream contribution | stretch | open |
| D8 | Stock or overclocked, and the clock regime | every measurement | **RESOLVED 2026-09-17.** MSI Dragon Center user scenario **"Balanced"** for the life of the project, **graphics clock locked at 1365 MHz**. Memory clock cannot be locked on this device, and whether Balanced is stock is UNVERIFIED, so the mitigation is **PARTIAL**. Pre-decided by the operator; Stage 0 chose and applied the lock value. See §2.1 |

## 2. Open questions requiring verification

| # | Question | Tag | Blocks |
|---|---|---|---|
| Q1 | 1650 Ti SM count, clocks, bus width, achievable bandwidth | **RESOLVED 2026-09-17** `[queried]`/`[measured]` | **CLOSED.** 16 SM; rated clock 1485 MHz, max 2100 MHz, **operating clock locked at 1365 MHz**; 128-bit bus; memory clock 6001 MHz; theoretical bandwidth 192.032 GB/s; **measured achievable 170.882 GB/s, efficiency 0.8899**. `HARDWARE.md` §1 |
| Q2 | Widest SIMD ISA on this CPU | **RESOLVED 2026-09-17** `[queried]` | **CLOSED. AVX2 + FMA3**, by CPUID leaf 7 EBX bit 5 with YMM state enabled; AVX-512F bit absent, ZMM state not enabled. Compiled `/arch:AVX2`; the benchmark asserts compiled-ISA == widest-supported-ISA. ~~**Caveat for Stage 6: the vectorised peak figure itself is INVALID** (std dev 5.86% / 5.60% across two runs) and `HARDWARE.md` §2 leaves it empty~~ — **CAVEAT LIFTED, Stage 0b 2026-09-18. The measured SIMD peak now EXISTS: 48.411 GFLOP/s, std dev 2.773%, VALID, and VALID in both Stage 0b runs** (run 1: 49.506 GFLOP/s, 1.075%). Vectorised-over-scalar 5.65x. `HARDWARE.md` §2 is populated. Stage 6 has the measured ceiling its framing requires |
| Q3 | GPT-2 small config values — confirm against shipped config, never hardcode from memory | **RESOLVED 2026-09-18** `[doc]` | **CLOSED. All five values CONFIRMED against the config shipped with the weights.** Stage 0 read `n_embd` 768, `n_head` 12, `n_layer` 12, `n_ctx` 1024, `vocab_size` 50257 from the published `config.json` of `openai-community/gpt2` (fetched 2026-09-16) and built the entire cuBLAS prefill denominator on them. Stage 1 re-read all five from `models/gpt2/config.json`, a **different artifact**, and every one is identical — so the five (N, K) shapes stand unchanged and **microbenchmark 6 was NOT re-run**; Stage 1 ran no timed benchmark of any kind. Corroborated twice: the reference tokenizer reports vocabulary 50257, and the enumerated `wte.weight` is [50257, 768]. Evidence and the value-by-value table: `MEASUREMENTS.md`, Stage 1 entry |
| Q4 | **Can Nsight Compute collect counters on this machine?** | **RESOLVED 2026-09-17** `[measured]` | **CLOSED. Yes — fully.** ncu 2025.4.0.0. All 32 required metric names resolved against the installed version, and **all 32 populated with values on a live kernel** (microbenchmark 1); zero unavailable, zero unresolved, nothing blank or defaulted to zero. Mapping and per-counter status in `HARDWARE.md` §4 and `bench/results/profile_gpu_bandwidth.json`. Note: collection for a **non-elevated** user was established by the operator pre-session; the Stage 0 session itself ran elevated and did not re-test that |
| Q5 | Confirm compute capability is sm_75 | **RESOLVED 2026-09-17** `[queried]` | **CLOSED. 7.5 confirmed**, agreed independently by `cudaGetDeviceProperties` (major.minor = 7.5) and `nvidia-smi --query-gpu=compute_cap` (7.5). The no-`cp.async` / no-`ldmatrix` / no-`mma` rule is now a hard constraint in §3, not an expectation |
| Q6 | Does the primary GPU report tensor cores? | **RESOLVED 2026-09-17** `[measured]` — **the expectation was WRONG** | **CLOSED, with a divergence.** No property reports them: `cudaDeviceProp` has **no tensor-core field** and CUDA exposes no API, so this cannot be answered by query at all. Determined empirically: a WMMA 16x16x16 HMMA kernel compiled for `sm_75`, ran, and returned all 256 elements exactly 32.0; `sm__inst_executed_pipe_tensor_op_hmma.sum` = 4 and `sm__pipe_tensor_cycles_active.sum` = 512. **A tensor pipe exists on this die and executes `hmma`.** No throughput was measured and none may be claimed — 128 cycles/instruction cannot separate full-rate silicon at low occupancy from a reduced implementation. **`cublasSetMathMode(CUBLAS_TENSOR_OP_MATH)` returns SUCCESS on this card and is NOT a valid tensor-core test** — recorded so it is not repeated. **Stage 11 must revisit its framing** (see §8, item W2) |
| Q7 | Free-tier GPU quotas (Colab, Kaggle) and rental pricing | `[training]` ⚠️ changes often | secondary comparisons |
| Q8 | Published roofline baseline ~34% average error on DL kernels (NeuSight, arXiv 2407.13853) | `[training]` ⚠️ verify before citing | Stage 10 positioning |
| Q9 | **Current GPU clock offsets.** MSI Dragon Center has been used to apply core and memory offsets. Exact values unknown | **STILL OPEN — UNVERIFIED, and NOT closable by query on this machine** | **Q9 CANNOT BE CLOSED.** `nvidia-smi -q` contains no clock-offset field at all (zero matching lines for Offset / Locked / GpuLock); Applications Clocks and Default Applications Clocks return "Requested functionality has been deprecated". Stage 0 additionally retried the vendor WMI path **with elevation** (previously Access denied): `root\WMI MSI_VGA` reads but returns 18 unlabeled integers with no schema and no offset field; `MSI_ACPI` returns "Not supported". Supporting evidence only: MSI user scenario **"Balanced"** `[operator-observed]`, fan profile not applicable (no control exposed for that scenario), MSI Afterburner installed but not running with `CoreClkBoost=` / `MemClkBoost=` both empty. **Whether Balanced is stock remains UNVERIFIED. No offset of zero is recorded anywhere, and none may be.** Mitigated, not resolved, by the D8 clock lock |
| Q10 | Does VRAM pass an integrity check at current clocks? | **RESOLVED 2026-09-17** `[measured]` | **CLOSED. PASS.** 2048 MiB buffer, patterns `0x00`/`0xFF`/`0xAA`/`0x55`, 3 rounds each = 12 write/readback cycles, every one bit-exact with zero bad bytes, run immediately after the five-minute thermal load at 82–87 C. Compute determinism also bit-identical. **This is the ONLY evidence available on memory stability in this project, because the memory clock cannot be pinned** — treat it as such and re-run it if intermittent correctness failures ever appear |
| Q11 | Can clocks be locked on this card (`nvidia-smi -lgc` / `-lmc`)? | **RESOLVED 2026-09-17** `[queried]` | **CLOSED, split answer. Graphics clock: YES, with elevation** — non-elevated `-lgc` returns "The current user does not have permission to change clocks" with exit code 4, a permission limit. **Locked at 1365 MHz and verified in effect.** **Memory clock: NO — device-capability limit.** `-lmc` returns "Setting locked Memory clocks is not supported for GPU 00000000:01:00.0." with **exit code 0**; exit 0 plus that message means elevation would not help. **Do not attempt `-lmc` again.** Consequence: the clock regime is **partially, not fully, controlled** |
| Q12 | Time until clocks stabilize under sustained load | **RESOLVED 2026-09-17** `[measured]` | **CLOSED. 0.075 s under the lock**, and structurally zero because `-lgc 1365,1365` pins the minimum as well as the maximum, so the card idles at its operating clock and no ramp exists. Unlocked it is 3.502 s by the stabilization criterion, but that figure is misleading: the unlocked card never stabilizes, it decays 1815 → 1365 MHz over five minutes. **Adjusted warmup: 25 iterations**, arithmetic in `BENCHMARK_PROTOCOL.md` §4.2 |
| Q13 | Run-to-run noise floor of the microbenchmark suite | **RESOLVED 2026-09-17** `[measured]` | **CLOSED. Adopted floor 4.4%** (90th percentile). Full suite run twice; across the 49 configurations VALID in both runs the spread was **median 0.317%, mean 1.407%, p90 4.401%, max 19.913%**. Two qualifications travel with it: **small-M GEMM shapes are materially noisier and need a wider margin**, and the floor was measured on a machine with irreducible background load. **Stage 0b 2026-09-18 qualification — the floor is UNCHANGED at 4.4% and was deliberately NOT re-derived**, because Stage 0b did not re-run the full suite and could not. But the condition it was measured under has changed: Stage 0b closed part of the load and **Riot Vanguard was absent entirely**, so the Stage 0b condition is **strictly lighter** than Stage 0's. A floor measured under heavier load stays conservative under lighter load, and a conservative floor only ever suppresses claims — it never licenses one. The figure is therefore safe to keep using; a later stage that re-runs the full suite should re-derive it. Stage 0b adds a third qualification to the two already travelling with it: **sub-100 us configurations are noisier still** — see the Stages 4/7/8 flag in §7 |

## 2.1 D8 — STOCK OR OVERCLOCKED. Decide in Stage 0, before Stage 1.

MSI Dragon Center has been used to apply core clock and VRAM offsets. This must be resolved before any inference code exists. Three risks, increasing in severity:

1. **Ceilings become an overclocked card's ceilings.** Survivable if fixed and documented. Fatal if the profile changes mid-project — a Dragon Center profile that does not persist across reboots, or a different shift mode on battery, means different stages measured different machines and the waterfall is void.
2. **Variance.** Aggressive clocks drift more under thermal load, pushing runs past the 5%-std-dev invalidity threshold for reasons unrelated to the code.
3. **Silent numerical corruption.** Consumer GDDR6 has no ECC. An unstable memory overclock does not crash — it returns slightly wrong values. This would surface as intermittent correctness-gate failures that vanish on re-run, most likely around Stage 8, and would be attributed to kernel logic rather than to the memory clock. Days lost debugging the wrong thing.

**Recommendation: run stock for the project's duration.** Nothing is gained here — the reported figures are ratios against measured ceilings, not absolute performance — and an entire class of confusing failure disappears. If the overclock is kept, it must pass the `HARDWARE.md` §5.4 stability checks, be documented exactly, and stay unchanged for the life of the project.

**RESOLVED 2026-09-17. D8: MSI Dragon Center user scenario "Balanced" for the life of the project, with the graphics clock locked at 1365 MHz.** Pre-decided by the operator and not reopened by Stage 0; Stage 0's job was to choose the lock value from measurement and apply it.

**The mitigation is PARTIAL, and that must not be forgotten.** Three things remain true:

1. **Whether Balanced is stock is UNVERIFIED** and cannot be verified on this machine — see Q9. No offset of zero is recorded.
2. **The memory clock cannot be locked** on this device (Q11), so only the graphics clock is pinned. Risk 3 above — silent numerical corruption from an unstable memory clock — is therefore mitigated only by the §5.4 VRAM integrity check, which **passed** under thermal load. That check is the sole evidence available and should be re-run at the first sign of intermittent correctness failures.
3. **The lock value was chosen from measurement, and the specified criterion could not be met.** The criterion was the highest clock held for five minutes with no throttle reason appearing; on this card the SW power cap asserts at t = 3.5 s and never clears, so no such clock exists. The highest clock actually *sustained* across the window was taken instead: 1365 MHz. The choice is vindicated — at 1365 MHz the card ran five minutes with **no throttle reason of any kind**, 122 of 122 samples at exactly 1365 MHz, peak 82 C.

## 3. Hard constraints

- **sm_75 gate — CONFIRMED BY QUERY 2026-09-17, no longer an expectation.** Compute capability 7.5, agreed by `cudaGetDeviceProperties` and `nvidia-smi`. No `cp.async`, no `ldmatrix`, no `mma` intrinsics anywhere. These require sm_80+. Stage 9 flash attention uses plain shared-memory staging. Any prompt proposing them is wrong. **This holds even though a tensor pipe was measured to be present on this die (Q6) — the prohibition is a project constraint, not a hardware inference.**
- **Primary device never changes.** Headline figures are ratios against this device's own measured ceilings.
- **4GB VRAM.** Constrains model size and the flash attention sweep's maximum sequence length.
- **Environment fingerprint frozen after Stage 0.** Driver version, CUDA toolkit, compiler, clock offsets, power limit, and Windows power plan are fixed. Every stage re-verifies before its first timed run. Any change voids comparisons against prior stages until reverted or re-measured.
- **No stage claims a speedup smaller than the Stage 0 noise floor, measured at 4.4%.** A 3% gain on a machine with 4.4% run-to-run spread is noise. Small-M / decode-shaped work is noisier still and needs a wider margin.
- **The graphics clock must be locked before every timed run, in every later stage.** Clock locks do not survive a reboot. Run `nvidia-smi -lgc 1365,1365` from an **elevated** shell, then verify with `python bench/machine_state.py verify-lock --mhz 1365` — never assume it took. Do **not** attempt `-lmc`; it is unsupported on this device. Unlocked, this card decays from 1815 MHz to 1365 MHz over five minutes, so an unlocked measurement is a measurement of a different machine at its start than at its end.
- **All project Python runs through `.venv/Scripts/python.exe`.** A bare `python` on PATH resolves to a different system interpreter with no torch installed.

## 4. Known traps

- **Porting only the matmul to CUDA** makes Stage 7 a measurement of PCIe bandwidth. Weights stay resident; the whole forward pass moves.
- **Stage 11 may be slower than Stage 8.** Expected. Do not tune until it wins; do not build the dashboard or resume bullet assuming a monotonically improving waterfall.
- **Do not claim tensor cores are weight-stationary systolic arrays.** They are documented as units performing small matrix multiply-accumulates. The systolic framing is a TPU analogy.
- **Self-relative speedups are not headline numbers.** A slow baseline inflates them.
- **Naive online softmax loses precision at long sequences.** Stage 9 must verify specifically at the longest tested length, not just at short ones.
- **Do not fit the performance model to Stage 11.** Stage 11 is its prospective test. Any term added after seeing Stage 11 data must be stated, and Stage 11 then stops counting as prospective.
- **Stage 2 is slow by design.** A long runtime there is not a hang. Use reduced token counts for correctness tests and state that you did.
- **Nsight counter access may be blocked on Windows.** Discover this in Stage 0, not at Stage 8.
- **An unstable memory overclock looks like a kernel bug.** Intermittent correctness failures that vanish on re-run are the signature. If they appear at any stage, check clocks before debugging logic.
- **Overlay and monitoring software consumes GPU.** Dragon Center, Afterburner overlays, Discord, and browsers are the usual offenders. Enumerated in Stage 0, closed before every timed run.
- **Some background load on this machine is irreducible and it shows up as INVALID runs, not as slow runs.** Riot Vanguard (a kernel-mode anti-cheat driver), Nahimic (an audio driver that hooks process creation), MSI vendor services and Windows Defender real-time scanning were all resident during Stage 0 and could not be closed. The signature is a configuration that is VALID in one suite run and INVALID in the next **with medians agreeing to well under one percent** — an occasional multi-millisecond interruption inflating the standard deviation. Expect it, report it as invalid, and do not chase it as a code defect.
- **An unpinned single-threaded CPU benchmark measures two machines and reports the mixture as variance.** Stage 0b found `cpu_simd_peak` INVALID in both Stage 0 runs with its excursions running **downward** — samples *faster* than the median, which interference cannot cause. The cause was thread placement: on 4 physical / 8 logical cores an unpinned thread sometimes owns a physical core and sometimes shares one, and L1d and L2 are per-core so a migration costs a full re-warm. Pinning to one logical CPU and raising priority moved the median to the **old minimum** and made both runs VALID. **Any single-threaded CPU measurement in a later stage must pin its thread and record where it ran** — `bench_pin_current_thread()` in `bench/microbench/bench_common.h` does both. Check deviation DIRECTION before assuming interference: upward from a clean floor is contention, downward from a ceiling cannot be.
- **`cudaDeviceProp::clockRate` and `::memoryClockRate` were REMOVED in CUDA 13.** Read them via `cudaDeviceGetAttribute(cudaDevAttrClockRate / cudaDevAttrMemoryClockRate)` instead. `bench/microbench/device_info.cu` already handles this; a naive re-query is the single most likely thing to break.
- **`nvidia-smi --query-gpu=power.limit` returns `[N/A]` on this card.** The power-limit figures come from `nvidia-smi -q`.
- **The reported 92 C slowdown temperature is not where this card starts losing clock.** The driver asserted SW thermal slowdown at **86 C**. Use the measured figure when reasoning about thermal headroom.
- **cuBLAS SGEMM throughput is non-monotonic in M on this device** — it peaks at intermediate M and falls at M=1024 for the same shape. The prefill denominator is therefore **shape-dependent and must be quoted per shape, never as a single number.** Stage 0's counters do not establish the cause; confirming it needs per-kernel profiling across the M sweep.
- **Background load is a recorded run condition, not a checklist item.** Defender (MsMpEng), Riot Vanguard (`vgc`, `vgtray`), WmiPrvSE, SearchIndexer, nvcontainer, dwm and Claude Code itself cannot be closed and were present for every Stage 0 measurement. The 4.4% p90 noise floor was measured with them running, and 17 of 96 configurations were invalid in both suite runs with 30 more invalid in one. That process population is part of the measurement condition and is recorded in each stage's entry per `BENCHMARK_PROTOCOL.md` §4. If a later stage's set differs from Stage 0's, the floor may no longer apply and the difference must be stated.
- **A zero exit code does not mean a push succeeded.** Stage 0 saw `git push` hang on an interactive credential prompt while the wrapper reported exit 0 and the log read `fatal: could not read Username` — Git Credential Manager resolved the unqualified remote host to a stored account that was not the repository owner. Every push is confirmed with `git ls-remote` against the branch, comparing the remote ref to local HEAD, never by exit status.

## 5. Corrections to the original capstone spec

Errors in `capstone_project_spec.md`, superseded by these foundation documents. Recorded so they are not reintroduced.

1. Treated matmul as uniformly compute-bound and prescribed cache blocking as the fix. Decode is memory-bound; blocking barely helps it.
2. Contained no KV cache.
3. Described porting only the matmul to CUDA.
4. Asserted the CUDA systolic dataflow is "how tensor cores actually work." Not supported.
5. Cited "Marvell AI Infrastructure" as a top-ranked reachable role justifying the project. **No such role exists in the targets tracker — zero rows.** Marvell's actual openings are firmware, DV, DFT, validation, reliability. The project is correct for other reasons; this justification is unsupported and must not be re-cited.
6. Assumed the project was unsaturated. It is a known genre with a public course (`tiny-vllm`) teaching it, and Georgia Tech CS 8803 sets nearly this exact capstone. Hence the differentiators in `PROJECT.md` §5.
7. Contained no flash attention. Its absence in a serious inference engine is conspicuous.
8. Framed the prediction discipline as the project's differentiator. It is a learning mechanism, not a resume feature. The differentiator is the Stage 10 performance model — the same intellectual content, built as a program instead of a journal.

## 6. Session log

| Session | Stage | Date | Commit | Outcome | Flags raised |
|---|---|---|---|---|---|
| 1 | 0 | 2026-09-16 | none | Recon; authored the Stage 0 code tree on branch `stage-0`. Nothing committed | — |
| 2 | 0 | 2026-09-16 | none | Re-ran the offline gate; fixed `test_cpu_cache_ladder` (replaced an unsound point-to-point monotonicity assertion with a dynamic-range check). Stopped at the hard checkpoint on operator instruction | — |
| 3 | 0 | 2026-09-17 | see PR | **Stage 0 COMPLETE, run unattended and elevated.** Offline gate green (53/53 build, 13/13 tests). Clock locked at 1365 MHz and verified. Nine microbenchmarks run twice. Profiler verified per counter. `HARDWARE.md`, `BENCHMARK_PROTOCOL.md`, `MEASUREMENTS.md`, `PERSISTENT.md`, `LEARNING.md` populated | Q6 tensor-core divergence → **Stage 11 must re-frame**; Q3 shape re-confirmation → **Stage 1**; D3 prompt set → **Stage 3**; single-channel RAM → **Stages 5 and 6**; Balanced-plan re-test → **before Stage 5**; Q9 remains open and unclosable |
| 4 | 0b | 2026-09-18 | see PR | **Stage 0b COMPLETE.** Scoped diagnostic and remeasurement of the two INVALID CPU microbenchmarks. Offline gate green (clean build, **14/14 tests**). Phase 1 diagnosed from the 192 retained Stage 0 per-sample arrays with no machine time; Phase 2 found a live CPU frequency source and no live temperature source; Phase 3 took the code branch — thread pinning and priority in both benchmarks — and remeasured twice. **`HARDWARE.md` §2 measured SIMD peak is now POPULATED at 48.411 GFLOP/s (VALID in both runs). Measured DRAM bandwidth REMAINS EMPTY** with a better-evidenced reason. All five ladder values replaced per operator decision 1. Corrected an M=1 validity error in the Stage 0 entry. Wrote the fingerprint file Stage 0 never committed | SIMD caveat on Q2 **LIFTED**; fingerprint flag **CLOSED**; new flags for **Stages 4/7/8** (decode-shape timing), **Stage 5** (temperature still blocked, frequency unblocked), **§6 CPU counters** (xperf PMU available, deliberately unused), and the **condition-difference** flag on the noise floor |
| 5 | 1 | 2026-09-18 | see PR | **Stage 1 COMPLETE.** safetensors loader and byte-level BPE tokenizer in C, with the reference oracle and the adversarial round-trip corpus. Offline gate green (clean build, **17/17 tests**). **Q3 CLOSED** — all five architecture values confirmed against the config shipped with the weights, so microbenchmark 6 was NOT re-run and this session timed nothing. 160 of 160 tensors compared against the reference EXACTLY, byte for byte. Round trip verified both ways across 48 records in eight classes: 48/48 decode byte-exact, 410 ids matching the reference element for element. Tensor inventory committed at `src/gpt2_tensor_inventory.json` | Q3 **CLOSED**; the LM head is **tied, not stored**; twelve square attention projections **unresolved by shape** and orientation unverified for every 2-D weight until Stage 2; causal-mask buffers recommended for regeneration; a first parameter/buffer byte split was **wrong and is corrected in the entry**; `TECHNICAL_SPEC.md` §4 amended to describe the repository; `CLAUDE.md` dangling filename fixed after the operator's rename |
| 6 | 2 | 2026-09-19 | see PR | **Stage 2 COMPLETE.** Naive C forward pass — embeddings, 12 pre-layernorm blocks, multi-head causal attention, gelu_new feed-forward, tied head, greedy selection, triple-nested `ijk` matmul behind a substitutable interface — plus the PyTorch reference oracle, which did not exist before. Offline gate green (clean build, **20/20 tests**). Prediction committed alone in `b33cb35` before any implementation file existed and sealed until the gap analysis. Seven timed configurations at 25 warmup and 30 samples: **prefill 15520.584 ms at L=64**, **decode 6561.243 / 13193.109 / 26842.550 ms at c=32/64/128**, isolated GEMM 1.5692 GFLOP/s L3-resident; **two INVALID and named** (prefill L=32 at 7.031%, GEMM N=3072 at 8.212%). Measured against predicted: **1.26× at prefill, 1.52–1.56× at decode**, all inside the prediction's own 2× band, with the whole gap in the efficiency fraction (0.120 measured at prefill, 0.098 at decode, against 0.150 assumed). Correctness: greedy sequence matches the oracle exactly, max absolute logit divergence **3.128e-04**, top-1 agreement at every position, **no tolerance adopted**. Orientation of the twelve square `attn.c_proj` tensors **selected by measurement** (3.128e-04 as-stored against 121.468 transposed). Causal mask **regenerated, not loaded** | **W11** and **W12** raised; **D2 and D3 deliberately left open**; scalar codegen proven from the emitted assembly; `HARDWARE.md` §2 scalar dispersion corrected 2.773% → 1.847%; `BENCHMARK_PROTOCOL.md` §2 timing function amended to `QueryPerformanceCounter`; `TECHNICAL_SPEC.md` §4 amended for four new paths; `.gitattributes` added outside the declared outputs to fix a Stage 1 test that CRLF checkout broke |

## 7. Flags for the next session

*Cleared and rewritten each session.*

*Items owned by a stage more than one session away do NOT belong here — they belong in §8, which is not cleared. This section carries only what the immediately following session needs. Where an item has been moved to §8, the line below is a pointer to its ID and not a copy of its text: a fact recorded in two places will eventually disagree with itself.*

**Raised by Stage 2, 2026-09-19. Everything here is for Stage 3.**

- **D2 IS STILL OPEN AND STAGE 2 DELIBERATELY SET NO TOLERANCE.** `BENCHMARK_PROTOCOL.md` §5 is unchanged and carries no threshold. Stage 2 measured the divergence instead and handed forward a distribution: **maximum absolute difference 3.128e-04 at both tested lengths, RMS 9.16e-05 (L=8) and 7.65e-05 (L=16), maximum relative difference 8.70e-06 against a denominator of |reference| + 1e-6 with zero elements below that floor, top-1 and top-5 set agreement at every position.** Full statistics, including percentiles at p50/p90/p99/p99.9/max for both absolute and relative difference, are in the Stage 2 `MEASUREMENTS.md` entry and as structured fields in `bench/results/stage2/stage2_correctness.json`. **The figure to set D2 from is the MARGIN**: the gap between the top-1 and top-2 reference logits was **0.0323 minimum at L=8 and 0.0144 at L=16**, against a worst-case divergence of 3.128e-04 — headroom of 103× and 46×. **The margin halved between the two lengths while the divergence did not move**, so a tolerance chosen at short length need not hold at long length. A threshold Stage 2 invented and then passed would have been circular; the distribution is the input, the decision is Stage 3's.
- **D3 IS STILL OPEN AND STAGE 2'S INPUTS WERE PLACEHOLDERS.** They are `tests/fixtures/stage2_placeholder_prompts.tsv`, labelled PLACEHOLDER in the fixture header, in the entry, and as structured fields in both results files. The consequence for the cross-stage waterfall is **§8 W11**, which Stage 3 owns — pointer only, the text is there.
- **THE MATMUL INTERFACE STAGES 3, 5 AND 6 BUILD AGAINST** is `src/gemm/gemm.h`, and it is fixed:

  ```c
  void gemm_naive   (int M, int N, int K, const float *A, int lda,
                     const float *B,  int ldb,  float *C, int ldc);   /* B  is K x N */
  void gemm_naive_bt(int M, int N, int K, const float *A, int lda,
                     const float *Bt, int ldbt, float *C, int ldc);   /* Bt is N x K */
  typedef struct { const char *name; gemm_fn mul; gemm_bt_fn mul_bt; } gemm_impl;
  ```

  Row-major, explicit leading dimensions so a sub-block of a wider buffer can be passed (attention does exactly that, one head at a time), `C` written rather than accumulated, no bias and no activation fused in. A replacement is substituted through `model_set_gemm(model *, const gemm_impl *)` and **touches no arithmetic in the forward pass**. Two operations rather than one because the file forces both: weights are stored `[input, output]` and the tied head is `[output, input]`. **Loop order at Stage 2 is `ijk` with a one-accumulator dot-product inner loop** — interchange to `ikj` is Stage 5's gain and was deliberately not taken.
- **THE OBSERVED PER-TOKEN DECODE COST, AND W3.** A decode step with no KV cache: **6561.243 ms at c=32, 13193.109 ms at c=64, 26842.550 ms at c=128** (medians, 30 samples, 25 warmup, all VALID at 1.299%, 1.217% and 3.579% of median). Cost is **very nearly linear in context** (2.011× and 2.035× per doubling) — the curve Stage 4 exists to flatten. **This falls five orders of magnitude OUTSIDE W3's stated 30–90 µs band**, which was derived for microsecond-scale M=1 GEMM configurations, so W3's failure mode did not appear here and **W3 is unchanged**. Dispersion character, for whoever revisits it: c=32 mixed (IQR 1.541% of median, one robust outlier carrying 27.4% of squared deviation), c=64 broad (IQR 1.953%, zero outliers), c=128 spike-carried (IQR 1.648%, two outliers carrying 92.3%).
- **THE SCALAR-CODEGEN FINDING: MSVC DID NOT VECTORIZE THE NAIVE MATMUL, AND THIS IS CHECKED AT BUILD TIME RATHER THAN ASSUMED.** The build emits `build/asm/gemm_naive.asm` from the same source with the **same frozen flags**, and `tests/test_gemm_naive.c` asserts **zero `ymm` registers and zero packed floating-point instructions**, with scalar `vmulss`/`vaddss` present so the loop was not eliminated. **No flag was changed and no de-vectorizing pragma was needed.** Two consequences: Stage 6 still has its whole demonstration in front of it, and — noted for Stage 6's framing — **`/fp:precise` also prevented FMA contraction**, so the Stage 2 inner loop is a separate multiply and add rather than one FMA.
- **TWO CONFIGURATIONS WERE INVALID AND ARE REPORTED AS INVALID**: prefill L=32 (7.031% of median) and the isolated GEMM at N=3072 (8.212%). Neither was retried or averaged away. The second one matters to Stage 5: it was the configuration that would have separated a scalar issue-rate limit from a memory-hierarchy limit, so **that separation is NOT established**. What is established is that with the whole B operand inside L3 the naive loop reaches **1.5692 GFLOP/s, 18.3% of the measured scalar ceiling** — most of the shortfall is not capacity misses.
- **WHAT THE ARTIFACTS CONTRADICTED.** `HARDWARE.md` §2 carried the vectorised dispersion (2.773%) in the scalar figure's cell; the raw Stage 0b file says **1.8466%** for the scalar record, and that one cell was corrected in place. `BENCHMARK_PROTOCOL.md` §2 named `clock_gettime(CLOCK_MONOTONIC)`, which does not exist under MSVC while every results file records `QueryPerformanceCounter`; that one bullet was amended. `TECHNICAL_SPEC.md` §4 had no home for `src/gemm/gemm.h`, `bench/stage2_forward_bench.c`, `tests/fixtures/` or `.gitattributes`; all four were added to the existing tree.
- **`.gitattributes` WAS ADDED, OUTSIDE STAGE 2'S DECLARED OUTPUTS, AND STAGE 3 SHOULD KNOW WHY.** `core.autocrlf=true` on this machine rewrites `.tsv` fixtures to CRLF on checkout, and `tests/test_stage1_oracle.py` compares a regenerated fixture byte for byte — so that Stage 1 test failed during Stage 2's offline gate on a file nobody edited. `*.tsv text eol=lf` fixes it. **No committed content changed**: the blob hashes of both Stage 1 fixtures are identical at `HEAD`, at the Stage 1 commit, and as `git hash-object` of the current working copies.
- **THE ENVIRONMENT FINGERPRINT DID NOT VERIFY CLEAN, AND THE CAUSE IS UNESTABLISHED — see §8 W12**, which Stage 3 owns. In short: 25 of 26 fields matched, the one difference was `build_flags.BENCH_BUILD_TIMESTAMP`, every substantive field was unchanged so the comparison against prior stages stands, and **Stage 1 recorded 26 of 26 against the same stored file after its own clean build**. Stage 2 did **not** rewrite the stored fingerprint and no stage should.

**Standing obligations, unchanged and not owned by any one stage.**

- **EVERY STAGE WITH A TIMED RUN — lock the graphics clock before the first one.** `nvidia-smi -lgc 1365,1365` from an elevated shell, then `python bench/machine_state.py verify-lock --mhz 1365`. Locks do not survive a reboot. **Never attempt `-lmc`** (§2 Q11). Stage 2 did this and verified it: locked, 10 of 10 samples at 1365 MHz. The lock is a thermal control for a CPU measurement as much as a clock control for a GPU one — this laptop's CPU and GPU share one thermal solution.
- **EVERY STAGE — re-verify the environment fingerprint** with `python bench/machine_state.py verify` before the first timed run, and **never run `machine_state.py fingerprint` to "refresh" it**: that subcommand WRITES the stored file rather than printing it, and overwriting the reference destroys the only thing the check compares against. See §8 W12 for the disagreement Stage 2 found.
- **STAGE 3 — fix the prompt set and token counts (§1 D3)**, and see §8 W11 for what that decision does to Stage 2's baseline figures. Stage 0's M sweep and Stage 2's placeholder prose are both placeholders. **The Stage 1 round-trip corpus is NOT a candidate** — those strings exist to break the tokenizer.
- **Q9 stays open and is not closable on this machine** (§2). Never record an offset of zero.
- **Expect INVALID runs and report them as invalid.** Stage 2 had two of seven. Never average one away, never retry until a number looks acceptable.
- **A CPU frequency sampler exists and is proven** (`CpuTelemetrySampler` in `bench/machine_state.py`). Stage 2 reused it at the Stage 0b settings — 0.02 s interval, mask `0xf0`, clear of the measured thread's core and its SMT sibling, sampling from a separate process — and measured its own duty cycle at **0.469% of one core** over 122,428 samples. Reuse it rather than building one; it is not to be called inside a timed bracket. **There is still no live CPU package temperature source**, so a thermal contribution to any CPU measurement remains unmeasured.

**Deferred work lives in §8 and is not repeated here.** Pointers only: **W1** (empty CPU DRAM bandwidth, Stages 5/10) · **W2** (Stage 11 tensor-core re-framing) · **W3** (decode-shaped timing construction, Stages 3/4) · **W4** (xperf PMU counter source, unused) · **W5** (the 4.4% floor, not re-derived) · **W6** (Balanced-power-plan re-test, before Stage 5) · **W7** (single-channel CPU memory ceiling, Stages 5/6) · **W8** (Stage 6's measured SIMD ceiling) · **W9** (shape-dependent cuBLAS prefill denominator, Stages 3/10) · **W10** (online softmax precision at the longest sequence, Stage 9) · **W11** (Stage 2's placeholder inputs and the waterfall, Stage 3) · **W12** (build timestamp inside the fingerprint, Stage 3). **Stage 2 changed the Status of none of them, added W11 and W12, and corrected one dangling pointer in W2's Status cell.**

## 8. Deferred work items

*NOT cleared between sessions. This section is the register of work owned by stages more than one session away; §7 carries only what the immediately following session needs.*

*A session may add rows, change a Status, or close an item. It must not delete one, and must not rewrite another stage's row to suit its own convenience. Where an item's canonical text already lives elsewhere in this file — §1 open decisions, §2 open questions, §4 known traps — the row below **cites** it rather than copying it, for the same reason §7 now carries pointers: a fact recorded in two places will eventually disagree with itself.*

| ID | Item | Owning stage | Why it is deferred | What would close it | Status |
|---|---|---|---|---|---|
| **W1** | **Measured CPU DRAM bandwidth is still empty** in `HARDWARE.md` §2. Stage 0b re-ran the *fixed* cache ladder twice under reduced background load and **every DRAM-tier working set was INVALID again in both runs** (run 1: 17.36 / 21.46 / 9.54 / 6.16 / 5.35%; run 2: 16.57 / 11.77 / 8.32 / 8.06 / 7.23%). The dispersion is **broad rather than spike-carried** — zero robust outliers at 8, 16 and 32 MiB against interquartile ranges of **19.88, 20.75 and 10.70% of median**. That is shared-L3 and single-channel-DRAM contention, which **no change inside the benchmark can control**. **More samples cannot fix this:** the protocol test is a *dispersion* criterion, not a *precision* one — more samples estimate the median better and leave the dispersion exactly where it is | **Stage 5**, binding hardest at **Stage 10** | Stage 0b took the diagnosis as far as the benchmark can go. Every remaining route either changes what is measured or changes the protocol, and **neither is a diagnostic session's to decide**. Stage 5 is the first stage to produce a memory-bound CPU kernel and therefore the first that actually needs a denominator for one | Three **candidate** routes, recorded as candidates and not as a plan, **each unverified**: **(a)** a longer per-sample duration so contention averages *within* a sample rather than *between* samples — this **changes what is measured** and must be justified explicitly, not slipped in; **(b)** an **xperf PMU diagnostic run** (see W4) establishing the mechanism through `LLCReference` and `LLCMisses`, reporting the figure with its dispersion characterised rather than suppressed; **(c)** a **documented amendment to `BENCHMARK_PROTOCOL.md` §3** for memory-bound CPU configurations — a **project-level decision, not a stage's to take quietly**. **The decision belongs to Stage 5, on evidence.** **Leaving the field empty with its present reason remains a legitimate outcome** | **OPEN.** Why it matters: `PROJECT.md` §7 item 4 requires the Stage 10 performance model validated across all kernels, and **a model with no CPU memory-bandwidth term cannot be** |
| **W2** | **Stage 11 must re-frame its tensor-core justification.** Its case against cuBLAS SGEMM was made conditional on this card having no tensor cores. **That condition is false** — a tensor pipe is present on this die and executes HMMA (§2, Q6). **No tensor-core *throughput* was measured, so the re-framing cannot assume one**; 128 cycles per instruction cannot separate full-rate silicon at low occupancy from a reduced implementation. **The project prohibition on `mma`, `ldmatrix` and `cp.async` is unaffected** — it is a project constraint, not a hardware inference | **Stage 11** | Stage 0 established the divergence and **deliberately did not re-frame Stage 11**, which is not a measurement stage's call. Stage 11 is many sessions away | Stage 11 states its case against cuBLAS SGEMM on grounds that survive the presence of a tensor pipe, without claiming a throughput figure nobody measured. Canonical evidence: §2, Q6 | **OPEN.** §2 Q6 points directly here, to §8 item W2. |
| **W3** | **Decode-shaped timings will be INVALID roughly half the time under the current 30-sample construction, and that is arithmetic, not pessimism.** Stage 0b's Phase 1 analysis of the retained Stage 0 arrays found the M=1 GEMM configurations have medians of **30–90 µs**, with spike share of variance **0.78–0.99** on an interquartile range of only **1.1–4.5%** — tight baselines punctured by single events. One sample at +50% among 30 gives a standard deviation of **0.5 / √29 = 9.3% of median**, which is the observed magnitude. Empirically **all five M=1 shapes were INVALID in at least one of the two Stage 0 runs**, and which one fails is not stable between runs — a **stochastic hit rate, not a property of any shape** | **Stages 3 and 4**, bearing on the **Stage 10 small-kernel term** | Stage 0b flagged it and **changed no benchmark to address it**: `cublas_sgemm_ref.cu` is the prefill denominator for the whole project, and §1 D3 records its M sweep as a placeholder **Stage 3 replaces**. Fixing the construction before Stage 3 fixes the sweep would be work done twice | **The honest remedies are more timed samples or batched, amortised measurement. Loosening the 5% limit is NOT among them.** Any Stage 10 term fitted to decode-shaped timings must account for a measurement whose variance is dominated by a Poisson-like interruption rate rather than by the kernel | **OPEN.** Flagged, not decided |
| **W4** | **A CPU hardware counter source exists on this machine and is deliberately unused.** Windows Performance Toolkit is installed (`xperf.exe`); `xperf -pmcsources` enumerates the live Comet Lake PMU: **CacheMisses, LLCReference, LLCMisses, UnhaltedCoreCycles, UnhaltedReferenceCycles, InstructionRetired, BranchMispredictsRetired** and more, **maximum 7 selectable simultaneously**. VTune and Intel PCM are **absent** | **Any later stage needing CPU counter evidence** — most obviously whichever attempts **W1** | **Stage 0b did NOT use it during its protocol runs on purpose:** an ETW PMU trace is itself a substantial added load and would have changed the very condition that stage existed to clean up. Using it requires a **separate, non-timed diagnostic run** | A later stage runs it outside its protocol runs and reports the counters as counter evidence under `BENCHMARK_PROTOCOL.md` §6. The obvious first use is confirming directly that the W1 DRAM-tier dispersion is shared-L3 contention, via `LLCReference` and `LLCMisses` | **OPEN — available, not yet used** |
| **W5** | **The 4.4% noise floor has not been re-derived, and the condition it was measured under has changed.** Stage 0b's process set is not Stage 0's: Nahimic and Intel DSA were closed and confirmed absent; **Riot Vanguard (`vgc`, `vgtray`) was not running at all**, where it was resident throughout Stage 0; Logitech Options+ respawned after being closed and is recorded as present; `msedgewebview2.exe` ×6 was uncloseable and **equally resident during Stage 0**, so it is a shared condition rather than a difference; the MSI service stack was left running by deliberate operator decision (§1 D8, §2 Q9); Stage 0b added a CPU frequency sampler at **0.36% duty of one core** on cores the measured thread never touches. **Net: strictly lighter than Stage 0** | **The first stage that re-runs the full microbenchmark suite** | Stage 0b **did not re-run the full suite and could not** — it re-ran only the two failing CPU benchmarks — so it had no basis to re-derive the floor and deliberately did not | A stage that re-runs the full suite twice re-derives the p90 run-to-run spread against **its own** process set and states the new figure with its condition. Until then the reasoning that keeps 4.4% safe: **a floor measured under heavier load stays conservative under lighter load, and a conservative floor only ever suppresses claims — it never licenses one** | **OPEN.** The 4.4% figure remains in force and must not be treated as re-derived |
| **W6** | **The Balanced-power-plan re-test is half blocked.** On the unlocked card the GPU asserted a thermal throttle reason and lost **24.8%** of its clock under sustained load, which is what makes the re-test worth doing. Stage 0b proved each candidate CPU telemetry source moves or does not, rather than assuming: **a live CPU FREQUENCY source EXISTS** — PDH `\Processor Information(_Total)\% Processor Performance`, **12.5 µs per probe**, moving ~**30 percentage points** under an applied load (idle 167.8–176.2%, loaded 140.0–150.4%) and recovering. **No live CPU PACKAGE TEMPERATURE source exists** — `MSAcpi_ThermalZoneTemperature` is **constant under full 8-process load** and costs roughly **1.36 seconds per probe**, static and unusable twice over; `Win32_Processor.CurrentClockSpeed` is likewise a constant nominal 2496 MHz. This **reproduces and explains** Stage 0's static telemetry rather than merely repeating it | **Before Stage 5** | The question — does the Ultimate Performance plan contribute enough CPU heat to pull GPU boost down — is **answerable on the frequency axis and not on the temperature axis**, in either direction, on this machine | Either a re-test framed so that frequency alone answers it, or a live package-temperature source that does not yet exist here. **Do not change the power plan casually — it is a frozen fingerprint value and changing it mid-project voids comparisons against prior stages** | **OPEN — half unblocked by Stage 0b** |
| **W7** | **The CPU memory system is half as wide as the part numbers suggest, and Stages 5 and 6 are evaluated against that.** One 8 GiB DIMM, **single channel**, rated 3200 MT/s and running at **2933**, giving a theoretical ceiling of **23.464 GB/s** rather than ~46.9 GB/s. Cache blocking and SIMD are judged against the single-channel figure | **Stages 5 and 6** | The hardware fact is recorded in `HARDWARE.md` §2; the **framing instruction** — evaluate against 23.464, not 46.9 — existed only in §7 and would have been erased with it | Stages 5 and 6 state their ceilings against the single-channel figure explicitly, and never against a dual-channel number inferred from the DIMM's rating | **OPEN — standing framing constraint** |
| **W8** | **Stage 6's measured-versus-predicted SIMD gap is now computable.** `TECHNICAL_SPEC.md` §3 frames Stage 6 as predicted-ceiling-is-vector-width against measured, with the gap as the interesting part. Stage 0b populated the measured ceiling: **48.411 GFLOP/s**, std dev 2.773%, VALID, and VALID in both Stage 0b runs (run 1: 49.506 GFLOP/s, 1.075%) | **Stage 6** | The ceiling did not exist until 2026-09-18; Stage 6 is many sessions away | Stage 6 computes the gap and explains it. **Caveat that must travel with the figure:** it is a measurement of a thread **pinned to one logical CPU at ABOVE_NORMAL priority class with THREAD_PRIORITY_HIGHEST** — a per-core ceiling, which is what Stage 6 wants, but **not the same measurement Stage 0 attempted** | **OPEN — unblocked, ceiling available** |
| **W9** | **The cuBLAS prefill denominator is shape-dependent and the cause is unestablished.** Throughput is strongly non-monotonic in M: best observed at **M=512** (ffn_down, 2344.35 GFLOP/s) and *falling* at **M=1024** (1598.62 GFLOP/s) for the same shape, with a similar peak-then-fall in every shape family. **Stage 0's counters do not establish the cause.** The denominator must therefore be **quoted per shape, never as one number** | **Stage 3** (which fixes the M sweep, §1 D3) and **Stage 10** (which consumes the denominator) | Confirming the hypothesis — that cuBLAS switches kernels and tiling heuristics at shape thresholds, non-monotonically, on a 16-SM device where a large M can leave a partially filled final wave — needs **per-kernel profiling across the M sweep**, which Stage 0 did not do | Per-kernel profiling across the M sweep, or an explicit decision to carry the denominator per shape without explaining the mechanism. Canonical trap text: **§4** | **OPEN — recorded as an observation, not an explanation** |
| **W10** | **Online softmax precision must be verified at the longest tested sequence length, not only at short ones.** A naive online softmax loses precision as sequences grow, so a pass at short lengths is not evidence for long ones | **Stage 9** | The failure mode only appears at the lengths Stage 9 sweeps, and §1 D4 (sequence lengths for the flash-attention memory sweep) is still open | Stage 9 verifies specifically at its longest tested length and reports the divergence there as a result, not as a caveat. Canonical trap text: **§4** | **OPEN** |
| **W11** | **Stage 2's baseline latencies were measured on PLACEHOLDER inputs, and the waterfall is compared against them.** `BENCHMARK_PROTOCOL.md` §4 holds the prompt set and token counts constant across every stage or the comparison is void. Stage 2 is the baseline every later stage is measured against, and §1 D3 is still open and owned by Stage 3. If Stage 3 fixes a set that differs from Stage 2's placeholder, **Stage 2's numbers were measured on inputs no other stage uses** | **Stage 3** | D3 is Stage 3's decision and Stage 2 was not entitled to take it; Stage 2 nonetheless required inputs in order to time anything at all. It labelled them placeholders in the entry and in the results file rather than proposing them as the prompt set — the same handling Stage 1 gave its round-trip corpus | Stage 3, having fixed D3, does one of two things and says which: **(a)** re-runs the committed Stage 2 binary at the fixed prompt set under protocol conditions and records the result in the Stage 2 entry as a dated second measurement, leaving the placeholder figures in place with their labels intact; or **(b)** states explicitly that Stage 2's placeholder figures are excluded from the cross-stage waterfall, and why. **(a) is the cheap route and is available** because the Stage 2 code is committed, the environment fingerprint is frozen, and the placeholder labelling keeps the two runs distinguishable | **OPEN — raised by Stage 2** |
| **W12** | **A build timestamp inside the environment fingerprint makes a clean verify impossible after any rebuild.** `machine_state.py verify` compares 26 fields, one of which is `build_flags.BENCH_BUILD_TIMESTAMP`. Any stage that rebuilds — which is every stage that writes code — will differ on that field and can never report a clean match. The two ways out are both bad: the check gets ignored as "the usual difference", or the stored file gets rewritten to silence it, and **both defeat the purpose of a frozen fingerprint**, which is to make a real change impossible to miss | **Stage 3** | Stage 3 builds the harness and is the natural owner of the measurement tooling; Stage 2 found the problem while verifying and is not entitled to redefine what the fingerprint compares | Stage 3 decides **whether a build timestamp belongs in the fingerprint at all, or whether it belongs in a separate build-provenance record that the verify step REPORTS without COMPARING**, and states which. Evidence to start from: Stage 2 recorded 25 of 26 fields matching with only `BENCH_BUILD_TIMESTAMP` differing (stored `2026-09-18T06:56:02Z`, current `2026-09-19T09:38:41Z`), while **Stage 1 recorded 26 of 26 against the same stored file after its own clean build**. Two stages, one check, one file, different answers — **Stage 2 could not establish why and the cause is UNESTABLISHED**. Stage 2 did NOT rewrite the stored fingerprint | **OPEN — raised by Stage 2** |

**Items deliberately NOT duplicated here, and where they already live durably:**

- **D3 — prompt set and token counts** is an open decision in **§1**, which is not cleared. §7's Stage 0 line about it is left intact rather than pointed at a new ID, because creating a W-number for it would be the third copy of one fact.
- **Q3 — re-confirm the GPT-2 shapes against the shipped config** is owned by **Stage 1, the immediately following session**, so it correctly stays in §7; it is also durable as **§2 Q3**.
- **Q9 — GPU clock offsets** is durable as **§2 Q9** and is not closable on this machine.
- **Lock the graphics clock / re-verify the fingerprint before every timed run** are **recurring obligations**, not deferred work; they are hard constraints in **§3**.
- **The fingerprint gap Stage 0b raised and closed** is history, recorded in the **§6 session log** and in `MEASUREMENTS.md`. Nothing is owed.

